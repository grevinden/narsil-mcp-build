# forgemax — .deb пакет

Место для метаданных deb-пакета forgemax.

## Планируется

- `DEBIAN/control` — метаданные пакета
- `DEBIAN/postinst` / `DEBIAN/postrm` — скрипты

Пока пусто. Документация forgemax: [../../src/forgemax/README.md](../../src/forgemax/README.md).
