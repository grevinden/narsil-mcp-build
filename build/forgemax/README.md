# forgemax — сборка

Место для сборочной инфраструктуры forgemax (MCP gateway sandbox).

## Планируется

- Makefile для сборки `forge` CLI и `forge-server`
- Поддержка сборки `.deb` пакета
- CI-скрипты

Пока пусто. Смотрите [исходники forgemax](../../src/forgemax/README.md).
